/// حزمة الواجهة الموحّدة.
///
/// ★ لماذا تستورد الشاشات هذا الملفّ بدل material مباشرة ★
///
/// كي يكون تحويل الأرقام إلى العربية مفتاحاً واحداً لا ٣٩٢ موضعاً.
///
/// الملفّ يُصدِّر material كاملاً إلا `Text` و`RichText`، ويضع بدلهما
/// نسختين تحوّلان الأرقام حين تكون الواجهة عربية. فأي شاشة تستورد
/// `ui.dart` تحصل على السلوك بلا سطر إضافي فيها.
///
/// والاستبدال هنا لا في كل شاشة: تغيير قاعدة التحويل — أو إلغاؤه —
/// تعديل ملفّ واحد.
library;

export 'package:flutter/material.dart' hide Text, RichText;

export 'widgets/localized_text.dart';
